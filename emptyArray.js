// Q3: Empty an Array in js? Do not reset it to a new array, do not loop through to pop each value.
let arr = [1, 2, 3, 4];
console.log(arr); // [1,2,3,4]
console.log(arr.length); // 4
console.log((arr.length = 0)); // 0
console.log(arr); //empty
